# gimp-mcp packs — optional tool bundles loaded on demand by server.py.
# Each module imports shared infra from _core and registers @mcp.tool tools at
# import time, so importing a module = enabling its pack. See ../PACKS.md.
